name: Test installer

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Syntax
        run: bash -n install.sh tests/install.sh
      - name: Selection behavior
        run: bash tests/install.sh
